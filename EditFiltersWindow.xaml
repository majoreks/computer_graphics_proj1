<Window x:Class="cg1.EditFiltersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:cg1"
        mc:Ignorable="d"
        Title="CG1 - Functional filters editor" Height="900" Width="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Controls" Grid.Row="0" Margin="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Content="Save filter" Grid.Column="0" Margin="5"/>
            </Grid>
        </GroupBox>
        <GroupBox Grid.Row="1" Margin="5">
            <Polyline Points="10,100 100,200 200,30 250,200 200,150" Stroke="Black" StrokeThickness="2" Stretch="Fill" Margin="5"/>
        </GroupBox>
    </Grid>
</Window>
